/*import React from 'react';
import { View, Button } from 'react-native';

export default function Home({ navigation }) {
  return (
    <View style={{ flex: 1, justifyContent: 'center' }}>
      <Button title="Nintendo" onPress={() => navigation.navigate('Nintendo')} />
      <Button title="Sega" onPress={() => navigation.navigate('Sega')} />
      <Button title="Sony" onPress={() => navigation.navigate('Sony')} />
    </View>
  );
}
*/ 